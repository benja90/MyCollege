<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="horizontal"
    android:layout_width="match_parent"
    android:layout_height="?android:attr/listPreferredItemHeight"
    android:paddingLeft="@dimen/activity_vertical_margin"
    android:paddingRight="@dimen/activity_vertical_margin">
    <TextView
        android:id="@+id/student_list_score"
        android:layout_width="@dimen/list_studient_course_score_text"
        android:layout_height="@dimen/list_studient_course_score_text"
        android:layout_gravity="center_vertical"
        android:background="@drawable/score_circle"
        android:textColor="@android:color/white"
        android:gravity="center" />
    <TextView
        android:id="@+id/student_list_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:maxLines="1"
        android:textAllCaps="true"
        android:ellipsize="end"
        android:layout_marginLeft="@dimen/activity_horizontal_margin"/>
</LinearLayout>